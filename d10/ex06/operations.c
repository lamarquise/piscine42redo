#include "do_op.h"

//we can use this later, when we actually need to be able to do this more than once. for that other project, could also use table of pointers to these funcs then.


int		ft_add(int n1, int n2)
{
	return (n1 + n2);
}

int     ft_sub(int n1, int n2)
{
	return (n1 - n2);
}

int     ft_mult(int n1, int n2)
{
	return (n1 * n2);
}

int     ft_div(int n1, int n2)
{
	return (n1 / n2);
}

int     ft_mod(int n1, int n2)
{
	return (n1 % n2);
}
