void	ft_putchar(char c);
void	ft_putstr(char *str);

		//this would not be submited, so feel free to change as need be for testing

int		main()
{
	char	*str;

	str = "what the fuck!?";
	ft_putstr(str);
	ft_putchar('a');
	return (0);
}
